<tr>
  <td><%= ticket.train.number %></td>
  <td><%= ticket.full_name %></td>
  <td><%= link_to t('common.actions.show') , ticket %></td>
  <td><%= link_to t('common.actions.delete'), ticket, method: :delete,
                  data: {confirm: 'Are you sure?'} %>
  </td>
</tr>