<div class="navbar navbar-default navbar-static-top">
  <div class="container">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#">Tutu</a>
    <div class="navbar-collapse collapse navbar-responsive-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to t('tickets.my'), tickets_path %></li>
          <li><%= link_to t('tickets.search'), search_path %></li>
          <% if current_user.admin? %>
            <li><%= link_to t('.administrate'), admin_root_path %></li>
          <% end %>
          <li><%= link_to t('common.actions.logout'),
                          destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to t('common.actions.login'), new_user_session_path %></li>
          <li><%= link_to t('common.actions.signup'), new_user_registration_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>