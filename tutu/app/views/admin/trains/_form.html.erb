<%= form_for([:admin, @train]) do |f| %>
  <%= render 'common/errors', resource: @train %>

  <div class="field">
    <%= f.label :number, class: 'control-label' %>
    <%= f.text_field :number %>
  </div>

  <div class="field">
    <%= f.label :inverse_wagons_order, class: 'control-label' %>
    <%= f.check_box :inverse_wagons_order %>
  </div>

  <div class="field">
    <%= f.label :route, 'Select route', class: 'control-label' %>
    <%= f.collection_select :route_id, Route.all, :id, :name %>
  </div>


  <div class="field">
    <%= f.label :current_station, 'Select station', class: 'control-label' %>
    <%= f.collection_select :railway_station_id, RailwayStation.all, :id, :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
